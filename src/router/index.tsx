import React from 'react'
import { HashRouter as Router, Route, Switch} from 'react-router-dom'

const basicRouter = () => (
    <Router>
        <Switch>
            <Route/>
        </Switch>
    </Router>
)